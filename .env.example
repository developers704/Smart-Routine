# Copy to .env on the VPS (never commit .env). pm2 ecosystem env works too.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com
PORT=4173
# Optional: move persisted state elsewhere (defaults to ./data)
# ROUTINE_DATA_DIR=/var/lib/smart-routine
