{
  "name": "routine",
  "version": "1.0.0",
  "private": true,
  "description": "Shift-aware hospital calendar PWA",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "test": "node tests/scheduler.check.js && node tests/alarm-plan.check.js && node tests/alarms.check.js && node tests/notify-sync.check.js && node tests/sync-result.check.js && node tests/tick-gate.check.js && node tests/atomic-write.check.js && node tests/push.check.js && node tests/travel.check.js && node tests/patch-ios.check.js && node tests/server-smoke.check.js",
    "test:smoke": "node tests/server-smoke.check.js",
    "vapid": "node scripts/generate-vapid.mjs",
    "cap:sync": "npx cap sync",
    "cap:ios": "npx cap sync ios && node scripts/patch-ios.mjs && npx cap open ios",
    "cap:android": "npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.5",
    "@capacitor/app": "^8.1.1",
    "@capacitor/core": "^8.3.5",
    "@capacitor/haptics": "^8.0.2",
    "@capacitor/ios": "^8.3.5",
    "@capacitor/keyboard": "^8.0.5",
    "@capacitor/local-notifications": "^8.3.1",
    "@capacitor/splash-screen": "^8.0.2",
    "@capacitor/status-bar": "^8.0.3",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.3.5"
  }
}
